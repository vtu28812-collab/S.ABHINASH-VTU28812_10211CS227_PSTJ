class Solution {
    public boolean repeatedSubstringPattern(String s) {
        // Concatenate s with itself
        String doubled = s + s;
        // Search for s in the middle slice (excluding first and last character)
        return doubled.substring(1, doubled.length() - 1).contains(s);
    }
}
